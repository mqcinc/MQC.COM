<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- Make site responsive on mobile/tablet -->
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>MQC inc.</title>
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"
    />
    <link rel="stylesheet" href="/stylesheets/main.css" />
  </head>
  <body>
    <nav class="navbar navbar-default">
      <div class="container">
        <div class="navbar-header">
          <button
            type="button"
            class="navbar-toggle collapsed"
            data-toggle="collapse"
            data-target="#navbar"
            aria-expanded="false"
            aria-controls="navbar"
          >
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>

          <a class="navbar-brand" href="/"><img id="img1" src="/img/logo_final.jpg"></a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav nav-color">
            <li
              class="<%= typeof page !== 'undefined' && page === 'campgrounds' ? 'active' : '' %>"
            >
              <a   id="navbar" href="/campgrounds">Home</a>
            </li>
            <li class="<%= typeof page !== 'undefined' && page === 'about' ? 'active' : '' %>">
              <a  id="navbar" href="/about">About Us </a>
            </li>
              <li class="<%= typeof page !== 'undefined' && page === 'services' ? 'active' : '' %>">
                <a  id="navbar" href="/services">Our Services </a>
              </li>
            <li class="<%= typeof page !== 'undefined' && page === 'teams' ? 'active' : '' %>">
              <a  id="navbar" href="/teams">Our team</a>
            </li>
            <li class="<%= typeof page !== 'undefined' && page === 'emails/new' ? 'active' : '' %>">
              <a  id="navbar" href="/emails/new">Contact Us</a>
            </li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <% if(!currentUser){ %>
            <li
              class="<%= typeof page !== 'undefined' && page === 'login' ? 'active' : '' %>"
            >
              <a  id="navbar" href="/login">Login</a>
            </li>
            <li
              class="<%= typeof page !== 'undefined' && page === 'register' ? 'active' : '' %>"
            >
              <a  id="navbar" href="/register">Sign Up</a>
            </li>
            <% } else { %>
            <li><a  id="navbar" href="#">Signed In As <%= currentUser.username %></a></li>
            <li><a   id="navbar" href="/logout">Logout</a></li>
            <li><a  id="navbar" href="/emails">Emails</a></li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>
    <div class="container">
      <% if(error && error.length > 0) {%>
      <div class="alert alert-danger" role="alert">
        <%= error %>
      </div>
      <% } %> <% if(success && success.length > 0) {%>
      <div class="alert alert-success" role="alert">
        <%= success %>
      </div>
      <% } %>
    </div>
    <div class="container">
      <!--
  </body>
</html>
-->
  
  </body>
</html>
